# Postie — downloads and updates

This repository holds nothing but the installer, the release packages and `latest.json`, the feed every installed copy of Postie reads to find out whether it is out of date.

**Download:** the `PostieSetup.exe` attached to the latest release.

The source is not here.
